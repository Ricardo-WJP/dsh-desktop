# 全局指令、Go 目录与记忆页布局

## 分工与范围

全局指令草案、Go 目录修复、记忆页布局修复均明确以 `gpt-5.6-luna` / `max` 委派；主代理核对官方来源、审核补丁及全局规则、同步安装版并实际验收。未打包或修改版本号，未删除其他服务、凭据、会话或记忆。

## 全局指令

`C:\Users\1\.codex\AGENTS.md` 正文从 4147 字符精简至约 2515 字符（约减少 39%）。合并重复安全、路由、维护要求；按需选择技能，必要偏好选择才用原生卡片；普通任务不自动执行全机维护或按年龄清理资料。保留 Luna max 优先、简体中文、设计研究/PPTX方向、现有视觉及动效、真实验证、备份、目录和代理约定。

## OpenCode Go：事实与修改

2026-09-05 现场读取：

- [官方 Go 模型接口](https://opencode.ai/zen/go/v1/models)：匿名与现有账号认证读取均返回 HTTP 200、35 条；数据只有 ID 等目录字段，没有逐模型账号资格结果。
- [官方 Go 文档](https://opencode.ai/docs/go/)：端点表解析到 28 条。
- [Go 首页](https://opencode.ai/go)为精选展示，不应当作完整订阅模型列表。
- 客户端下方模型选择菜单还包含其他已接入服务，例如 DeepSeek、OpenAI Codex；不能将整个菜单都视作 Go 的模型。

旧逻辑按模型名称前缀猜测请求协议，令仅出现在 API 中、缺少当前官方路由说明的 7 个 ID 也被纳入。现改为实时 API IDs 与有效官方 Go 端点描述的交集；当前生成 28 条，不内置静态模型白名单。仅允许官方 HTTPS Go 域名及三类明确端点，SDK 文案不覆盖端点协议。文档缺失或网络故障时不猜新模型，保留旧缓存并返回陈旧缓存状态。

这不证明 28 个模型都已在本账号成功调用；此次模型推理请求为 0。区域限制、订阅余额和账号资格需要区分，不能由目录 GET 成功推断。

证据：`output/go-catalog-audit-before/report.json`、`output/go-catalog-audit-after/report.json`，通过 `scripts/inspect-live-go-catalog.mjs` 生成，仅含公开模型元数据。

## 记忆页布局

实际加载的是 physical profile 内 Mnemon 0.5.2，旧 candidate/profile 内 0.5.1 并非本次 UI 判断依据。记忆页为独立定位的 `section[data-dsh-mnemon-view]`，不继承聊天区的 AppFrame 可用宽度。

仅为该页面标记外置页面布局，保留原 left/top，以原生 `--dsh-sidebar-width` 和 `--dsh-sidebar-height` 控制 right/bottom。未加回 `#root` 全局缩窄、未强制关闭侧栏或修改动画开关。

真实桌面验收已验证：右侧栏开启时完整显示记忆页右侧状态及按钮；底部面板开启时记忆内容让位；将右侧栏从约 280px 拖至 540px 时内容同步收窄、不被遮挡，随后恢复原宽度与底部面板收起状态。主窗口本轮保持约 1570px，未将拖宽侧栏的测试冒称为改变系统窗口宽度。

## 验证与备份

- 全量测试：722 项，719 通过、0 失败、3 跳过。
- Go 目录定向测试：8/8；包含动态新增、目录移除、无文档、非法域名/路径等。
- 布局及相邻集成定向测试：25/25；实际窗口验证与数学/静态断言分别记录。
- client 源码/安装版/候选副本哈希一致：`EA9FE62D83926077535CCD99C3ADD00252D1EC2967182385DE04DA242FEEDB9F`。
- Go 目录模块源码/安装版哈希一致：`86C634D4B9646D9CA6C4CBF53BD44A4A8E49A470DB1EB5984F45A676A91F489B`。

备份目录：`C:\Users\1\.codex\backups\auto-config-upgrades\2026-09-05-195756`，包含修改前 AGENTS、src/test/scripts 及安装版/候选 client 和安装版 Go 目录模块。

只恢复本轮对应文件即可回滚代码；不要覆盖其他后续改动或通过恢复旧凭据/会话来回滚界面。

## 安装版最终状态

客户端正常退出后，通过原有候选验证/观察流程，于 2026-09-05 20:28:32 激活 `plugin-01a29400-b5fe-4adc-8297-0ad44a1318d8`。DSH 版本仍为 0.1.2-rc.1，桌面版仍为 0.1.55；没有新打包。

- active 与 last-known-good 一致，pending 和 plugin-candidate 事务记录均已清除。
- 实际 physical profile 与 runtime 中的 Go 目录都是 28 条；profile-source 中的 16 条是构建源副本，不是实际运行目录。审计脚本已区分三者，不能用源副本冒称实际加载状态。
- 重启后的约 1400px 窗口再次实测记忆页：右侧栏开启时不遮挡，收起时自动填满；最终恢复左右侧栏展开、底部面板收起并留在记忆页。
- Go 凭据引用与旧候选一致（仅比较，不输出内容）。当前会话所选模型未被修改。
- `output/go-catalog-installed-verified/report.json` 记录实际目录与实时官方目录比对。
- 普通运行启动部分耗时约 15.8 秒；本次候选克隆/验证/观察的整体切换耗时更长，不能把 15.8 秒冒称为整个更新耗时。
- 已知遗留：预检临时目录的 `electron/resources/default_app.asar` 因 EBUSY 占用导致延迟清理未完成；事务和工作区启动成功不代表临时清理也成功。本次不扩大清理范围或删除用户资料。
