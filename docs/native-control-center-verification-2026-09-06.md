# 原生控制中心与推荐清单验收

## 范围

用户选择“自动：按 DSH 规范选择”，落地 A 侧栏总览（cbf0029e）。沿用 DSH 深浅色、系统字体、线性图标和胶囊按钮。推荐页保持原样式，只调整推荐内容。未打包、未升级或删除已安装插件、未变更模型凭据与用户数据。

## 验证

- TypeScript 检查、Vite 构建通过。
- renderer/onboarding/plugin-suite 针对性测试共 16 项通过；运行日志保留修复后 renderer 的 8 项再次通过。
- 编译后真实 React + 隔离模拟 Host：深浅色 × 1400/760/520 宽 × 5 页面，共 30 个视图，无横向溢出、无捕获到的渲染错误。详见 `output/native-control-center-qa-final/report.json`。
- 模拟 Host 交互：重启重复提交阻止、失败反馈、快照取消不执行、恢复确认和目标 ID 绑定通过。没有执行真实数据恢复。
- 推荐页模拟验收：标准/套件 × 深浅色，共 4 种状态通过；实际安装未执行。详见 `output/onboarding-recommendations-qa/report.json`。
- 已同步至本机安装版并从工作区 Ctrl+Shift+M 打开：原生深色布局、真实 DSH 0.1.2-rc.1、桌面 0.1.55、工作区就绪及实际日志可见。运行详情实际点击展开，用户数据独立保存状态正常。
- 实机发现日志刷新先清空列表会改变页面高度。已修复为刷新期间保留现有条目，并重新构建/同步；关闭重开控制中心后，滚动到底部可正常展开详情。

## 生效与恢复

控制中心已在本机生效；未重启 DSH 主进程，工作区保持运行。推荐模块文件已同步，需下次正常启动加载；未重置首次启动标记。推荐项为 dshmarket、dsh-better-sidebar、dsh-notification；原完整套件清单保留。

安装版原文件备份：`C:\Users\1\.codex\backups\auto-config-upgrades\2026-09-06-203800-native-control`。回滚时将 renderer 备份恢复至安装目录 `resources/app/build/renderer`，将两项 onboarding 文件恢复至原位置，然后仅重新打开控制中心；推荐模块需下次正常启动加载。无需回退用户数据。

## 边界

窗口尺寸/深浅色组合在隔离 Electron 中验收；实机检查为当前窗口与深色主题。更新/恢复的破坏性路径只走模拟 Host，不声称进行过真实升级或恢复。未进行全仓库回归或性能基准。

独立 Luna max 视觉复核未在本轮返回结论，已停止等待；不得将其记为 ship 或已通过。主代理完成截图检查、实际客户端检查与上述测试，但不以此冒充独立复核。
