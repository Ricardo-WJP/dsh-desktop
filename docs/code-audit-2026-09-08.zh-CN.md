# 保持当前视觉的代码审查与回归

日期：2026-09-08。桌面版仍为 0.1.56。本轮不打包，不更新依赖或插件，不修改主题、字体偏好、布局、窗口材质、图标、按钮规格或正常交互。

## 结论

完成仓库自有代码范围的全量自动检查，以及启动/退出、进程归属、IPC/导航、插件事务、恢复、前端异步生命周期等重点人工审查。只修改两个生产文件中的三个异常路径。没有为了减少行数删除或重排现有 CSS，也没有重新设计页面。

这不等于逐行审完每个第三方依赖，也不等于保证所有环境零缺陷。Windows 安装版已同步修复并完成启动检查。

## 基线与验证

- 基线备份：`C:\Users\1\.codex\backups\auto-config-upgrades\2026-09-08-175824-code-audit`，包括 src、test、scripts、compatibility、依赖清单和当时的 renderer/preload 构建产物。
- 清点 243 个自有源文件、脚本、测试和兼容性文件；211 个 JS/CJS/MJS 语法检查通过，5 段 HTML 内联脚本解析通过。
- TypeScript 检查及 renderer 构建通过。构建结果没有覆盖安装版控制中心。
- 修改前：792 项测试，788 通过、1 失败、3 跳过。
- 最终全套：798 项测试，795 通过、0 失败、3 跳过。
- 跳过项是迁移、兼容性门禁、产物清单的三项文件符号链接测试，均因当前 Windows 创建符号链接返回 EPERM；未移除或降低这些测试。
- 隔离 Electron 验收通过，运行窗口隐藏、测试数据目录独立、网络请求为空。它是 fixture 模拟，不冒充真实插件安装或全端到端验证。
- 安装版普通启动完成，工作区可见；3080 监听存在、9338 调试监听已关闭。本次未实际安装插件、切换候选或执行用户数据回退。

证据：`output/audit-baseline-tests.log`、`output/audit-final-tests.tap`、`output/code-audit-inventory.json`、`output/code-audit-acceptance/run-1788862216777-56196-wySa1p/report.json`。

## 已复现并修复

1. **批量更新旧结果残留**：整批请求完成后，后续更新同名插件可能复用旧 candidate。现在保留本次响应的 batchSize，同时及时结束已完成批次；成功启用后也清理仍对应本次的批次，避免清掉新批次。新增测试先在旧代码失败，再在修复后通过；原并发合并、重复点击和失败不启用测试仍通过。
2. **更新按钮卸载竞态**：清理完成后，迟到的 checkUpdate 回包仍可能重新插入按钮。现在在异步回包后、修改 UI 状态前检查 disposed。新增延迟回包测试先失败后通过。
3. **推荐安装事务标识不匹配时卡住**：原代码直接返回，保留 busy 状态。现在通过现有错误处理恢复可操作状态、清除事务标识并提示重试。成功路径、样式与正常文案不变；新增异常回包测试先失败后通过。

生产文件仅为 `src/plugins/dsh-desktop-integration/lib/client.js` 和 `src/pages/plugins-onboarding.html`。这次精简的目标是状态生命周期，而非行数或代码体积；未测量或承诺内存、速度的百分比收益。

## 视觉保护

- 客户端布局/按钮 CSS 提取结果与备份完全一致。
- 自定义外观 CSS 与备份完全一致。
- 推荐页全部 style 块与备份完全一致。
- `src/renderer` 与 `src/window-host.js` 无本轮改动。
- 新增 `test/approved-visual-baseline.test.js` 锁定三组已确认样式的 SHA-256。未来有意修改视觉时，应先取得设计确认再更新基线，不能为了过测试随意刷新哈希。
- 旧隔离验收仍硬编码 188px 设置侧栏，与当前已确认的 208px 设计不一致，导致基线唯一失败。本次仅更新测试 fixture 到 208px，并将对话框宽度断言加强为 1060px；保留层级、遮挡、滚动和草稿保护断言，没有修改生产样式来迁就旧测试。

## 未贸然修改的审查项

- 子代理最初提出的跨进程恢复/发布竞争，经主代理要求复核后撤回生产高危定级：正常入口已有 release ownership lease 和 operation coordinator 串行保护。未重复加锁。
- 遗留 bundled-plugin 安装导出存在失败恢复改善空间，但当前生产入口不调用该路径；正常安装走隔离 candidate 事务。没有重写遗留流程或影响当前插件。
- `readProfileInputs` 接受 recipe 中可解析到仓库外的路径，属于可信本地构建输入的边界硬化建议；未证明远端数据能进入该入口。收紧前需明确是否支持外部 evidence，避免破坏构建用途。
- 恢复页重新加载时保留上一轮错误提示是待明确的交互细节，不按视觉重设计处理，本轮未改。
- 没有启用新自动化、修改系统安全设置、清理历史目录、改变模型或凭据。

## 回滚

安装版源文件和活动 profile 的 client.js 在同步前均核对与备份基线一致。

若需回滚本轮修复，只恢复备份中的上述两个生产文件，并把 client.js 同步到当前活动 profile 的同名适配器文件后，重载已核实的 DSH 进程。不要整体覆盖备份目录、reset 仓库或回退会话/插件数据；活动 candidate 路径应依据当前运行状态重新确认。测试 fixture 与新增回归测试可单独保留或对应恢复。
