/**
 * dsh-update-button host half.
 *
 * Update ownership stays in DeepSeek Harness Desktop's native Update route. The profile
 * plugin deliberately has no host route, process control, or filesystem
 * mutation; its client half only contributes the official sidebar slot.
 */
export const name = 'dsh-update-button'

export function apply() {}
